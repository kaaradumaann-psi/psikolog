diff --git a/scripts/diagnose-supabase.mjs b/scripts/diagnose-supabase.mjs
index 2a9ed0f..493d696 100644
--- a/scripts/diagnose-supabase.mjs
+++ b/scripts/diagnose-supabase.mjs
@@ -64,6 +64,7 @@ const EXPECTED_MIGRATIONS = [
   '20260919020000',
   '20260920000000',
   '20260923000000',
+  '20260925100000',
 ];
 
 const findings = [];
diff --git a/src/App.tsx b/src/App.tsx
index aba732c..92ce668 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -1,5 +1,6 @@
 import { useEffect, useState } from 'react';
 import { AuthGate } from './components/AuthGate';
+import { SsoConsumePage } from './components/SsoConsumePage';
 import type { AuthFlowOrigin } from './components/AuthGate';
 import { DesignPreviewPage } from './components/DesignPreviewPage';
 import { AdminPanel } from './components/AdminPanel';
@@ -109,6 +110,9 @@ export default function App() {
 
   // Bilgi sayfaları (SSS, Gizlilik & KVKK, Kullanım Koşulları, Kaynakça):
   // oturum ve yapılandırmadan bağımsız, kendi kabuğu ve alt bilgisiyle açılır.
+  if (route.page === 'sso') {
+    return <SsoConsumePage />;
+  }
   if (route.page === 'sss') {
     return (
       <InfoPageShell kicker="Yardım" title="Sıkça Sorulan Sorular" onBack={() => navigate('/')}>
diff --git a/src/router.ts b/src/router.ts
index bb793f0..8c67759 100644
--- a/src/router.ts
+++ b/src/router.ts
@@ -19,6 +19,7 @@ export type AppRoute =
   | { page: 'kullanim' }
   | { page: 'kaynaklar' }
   | { page: 'onizleme' }
+  | { page: 'sso' }
   | { page: 'bulunamadi' };
 
 /* ------------------------------------------------------------------ */
@@ -40,6 +41,7 @@ export function parseRoute(pathname: string): AppRoute {
   if (path === '/kullanim') return { page: 'kullanim' };
   if (path === '/kaynaklar') return { page: 'kaynaklar' };
   if (path === '/onizleme') return { page: 'onizleme' };
+  if (path === '/sso') return { page: 'sso' };
   const reportMatch = /^\/kayitlar\/([^/]+)\/raporlar(?:\/([^/]+))?$/.exec(path);
   if (reportMatch) return { page: 'raporlar', id: reportMatch[1]!, ...(reportMatch[2] ? { reportId: reportMatch[2] } : {}) };
   const kayitMatch = /^\/kayitlar\/([^/]+)$/.exec(path);
diff --git a/supabase/config.toml b/supabase/config.toml
index 0032077..f30cc1e 100644
--- a/supabase/config.toml
+++ b/supabase/config.toml
@@ -10,3 +10,6 @@ verify_jwt = true
 # aktiflik durumunu ve (kayıt modunda) kayıt sahipliğini yeniden doğrular.
 [functions.ai-interpretation]
 verify_jwt = true
+
+[functions.sso-consume]
+verify_jwt = false
diff --git a/tests/router.test.ts b/tests/router.test.ts
index 2f01efc..5a14b45 100644
--- a/tests/router.test.ts
+++ b/tests/router.test.ts
@@ -18,6 +18,7 @@ test('pathname router recognizes workspace and public information routes', () =>
   assert.deepEqual(parseRoute('/kullanim'), { page: 'kullanim' });
   assert.deepEqual(parseRoute('/kaynaklar/'), { page: 'kaynaklar' });
   assert.deepEqual(parseRoute('/onizleme'), { page: 'onizleme' });
+  assert.deepEqual(parseRoute('/sso'), { page: 'sso' });
 });
 
 test('pathname router preserves a record identifier and rejects extra path segments', () => {
