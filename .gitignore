# OMRChecker-master is a python reference only — never track or depend on it.
# The project must stay independent; keep the adaptation in pure TypeScript.
OMRChecker-master/
# Copyright-sensitive sample data — never track
Örnek Telefon Görüntüleri/
mmpiHesapİcinGerekliOlabilecekTurkStandardizasyonları/
# Scraped site assets must never be tracked
MMPI-566*.html
# Yerel kaynak arşivi: başka bir uygulamadan alınmış paket dosyaları, örnek
# çıktılar ve künyesiz tarama belgeleri. Yalnızca yerelde kalır; depoda izlenmez.
/yerel-kaynaklar/
/kaynaks/
6A-OLCUM-RAPORU.txt
C-SERISI-*.txt
DOGRULAMA.md
ENGINEERING_HANDOFF*.md
MIMARI-ANALIZ.md
MUHENDISLIK-RAPORU*.md

# optik-form.html is the single-file deliverable and is intentionally tracked so it can be
# handed over without a build step. It is regenerated by `npm run build` and
# tests/build.test.ts fails if the embedded form PDF drifts from the committed one.

node_modules/
dist/
.env
.env.*
!.env.example
supabase/.temp/
*.tsbuildinfo

# Wrangler yerel durumu ve yerel deploy değişkenleri — commit edilmez
.wrangler/
.dev.vars
.dev.vars.*

# Local diagnostic dumps — never commit
*.pgm
/crops/
/debug-out/
.DS_Store

# Gevici sonuç paneli önizleme çıktısı — derlenen statik önizleme
preview-dist/

# Denetim/önizleme çalışma alanı — commit edilmez
.audit/

# Yerel doğrulama overlay/ara çıktıları — commit edilmez
/scripts/validation/
