# P0.7 — LIVE SUPABASE + GERÇEK AUTH / RLS / STORAGE PRODUCTION DOĞRULAMASI

**Dal:** `arena/01a0d892-psikolog` · **Taban:** `d57ae22` (P0.6) · **Tarih:** 2026-09-25
**Kaynak raporlar:** `docs/P0.5-REPORT.md`, `docs/P0.6-REPORT.md`

---

## 1. Executive Summary

**Canlı Supabase doğrulaması yapılamadı.** Üç bağımsız engel ölçüldü:

| Engel | Ölçüm |
|---|---|
| Supabase uç noktaları erişilemez | `api.supabase.com`, `supabase.com`, `*.supabase.co` → **HTTP 000** |
| Access token yok | `supabase projects list` → `Access token not provided`; `SUPABASE_ACCESS_TOKEN` yok; `~/.supabase/` yalnızca `telemetry.json` |
| Project ref yok | `supabase/config.toml`'da `project_id` anahtarı **hiç yok**; repoda yalnızca `YOUR_PROJECT_REF` placeholder'ı |

**Supabase CLI çalışıyor** — bu fazda kuruldu ve gerçek komutlar çalıştırıldı (aşağıda). Engel CLI değil, ağ ve credentials.

Buna karşılık bu fazda **iki gerçek doğrulama** üretildi:

1. **Migration güvenliği** — `db push`'un taşıdığı iki risk PGlite'ta gerçekten test edildi: dolu bir
   veritabanına yeni migration uygulamak (veri kaybı yok) ve push'u tekrarlamak (idempotent).
2. **Production bundle'ın DOM'da gerçekten çalıştırılması** — `dist/assets/` içindeki gerçek dosyalar
   ES module olarak jsdom window'unda evaluate edildi, React mount oldu, access gate gerçekten koştu.
   Test **mutation ile doğrulandı**: gate bozulunca test FAIL oluyor.

```
npm run typecheck   → temiz
npm test            → # tests 136  # pass 136  # fail 0     (baseline 130 → 136)
npm run build       → ✓ index 487.13 kB / gzip 136.86 kB
```

---

## 2. P0.6 baseline

Bu fazın başında, kod değiştirilmeden ölçüldü:

```
pwd                     /home/user/psikolog
git branch              arena/01a0d892-psikolog
git status              temiz (d57ae22)
npm run typecheck       temiz
npm test                # tests 130  # pass 130  # fail 0
npm run build           ✓ index-BS_3veIg.js 487.13 kB / gzip 136.86 kB
supabase/migrations/    10 dosya
```

**P0.6 kodu yeniden yazılmadı, geri alınmadı, yeniden tasarlanmadı.**
`20260925000001_p0_5_storage_object_isolation.sql` ve `20260925000002_p0_6_formulation_safety.sql`
dosyalarına **hiç dokunulmadı** (`git diff` bunu gösteriyor — değişen dosyalar §24'te).

---

## 3. Supabase project verification

### CLI

| Kontrol | Sonuç |
|---|---|
| `npx supabase --version` (kurulu değil) | `npx canceled due to missing packages` — CLI repoda kurulu değil |
| `supabase` npm paketi | 0.01 MB — yalnızca bir shim (`dist/supabase.js`) |
| Gerçek binary | **`@supabase/cli-linux-x64`** registry'de mevcut, kuruldu |
| `supabase --version` | **2.118.0** ✓ |

### Auth durumu

```
$ supabase projects list
Access token not provided. Supply an access token by running `supabase login`
or setting the SUPABASE_ACCESS_TOKEN environment variable.

$ supabase login
Cannot use automatic login flow inside non-TTY environments.
Please provide --token flag or set the SUPABASE_ACCESS_TOKEN environment variable.
```

- `env | grep -i supabase` → **boş**
- `~/.supabase/` → yalnızca `telemetry.json`, `traces` (credentials yok)
- `~/.config/supabase` → yok

**Kullanıcı zaten login olmadığı için login denendi** (§3 gereği) ve ortam non-TTY + token yok + API
erişilemez olduğu için mümkün değil.

### Project ref

`supabase/config.toml` satır 2:
```
# Fill project_id after `supabase link --project-ref YOUR_REF`
```
Dosyada `project_id` anahtarı **yok**. Repoda proje referansı araması:
```
supabase/README.md:12:  VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
supabase/README.md:22:  supabase link --project-ref YOUR_PROJECT_REF
supabase/config.toml:2: # Fill project_id after `supabase link --project-ref YOUR_REF`
```
`.supabase/` dizini yok (link edilmiş projenin kaydı orada dururdu).

# `BLOCKED — Supabase project ref doğrulanamadı`

§4 kuralı gereği **tahmin edilmedi, hiçbir projeye bağlanılmadı.** Yanlış projeye `db push`
production verisini bozabilirdi.

### Ağ ölçümü (bu fazda yeniden yapıldı)

| Hedef | Sonuç | Gerek |
|---|---|---|
| `api.supabase.com` | **HTTP 000** | login, `db push`, Management API |
| `supabase.com` | **HTTP 000** | auth akışı |
| `aaaaaaaa.supabase.co` | **HTTP 000** | proje veri/Auth/Storage uç noktası |
| `objects.githubusercontent.com` | HTTP 000 | CLI release binary'leri (npm üzerinden aşıldı) |
| `cdn.playwright.dev` | HTTP 000 | browser binary'leri |
| `deb.debian.org` | HTTP 000 | eksik sistem kütüphaneleri |
| `registry.npmjs.org` | HTTP 200 | izinli tek host |
| `api.github.com` | HTTP 200 | izinli |

Bearer header ile bile: `curl -H "Authorization: Bearer dummy" https://api.supabase.com/v1/projects` → **HTTP 000**.
Yani token olsaydı da API'ye ulaşılamazdı.

---

## 4. Migration status

### Canlı Supabase

# `BLOCKED`

`supabase db push` **çalıştırılmadı**. Sebep zinciri: project ref yok → `supabase link` yapılamaz →
push edilecek hedef yok. Ayrıca `api.supabase.com` HTTP 000.

Canlı veritabanında `formulations` / `safety_plans` / RLS / policy / trigger / foreign key varlığı
**doğrulanmadı**. "db push başarılı görünüyor" denmedi — push hiç yapılmadı.

### Local / PGlite (gerçek migration dosyaları)

Bu fazda **yeni** doğrulandı: `tests/migrationSafety.test.ts` → **5/5 PASS**.

| Test | Ne kanıtlıyor | Sonuç |
|---|---|---|
| `db push applies the pending migration without touching existing rows` | P0.6 öncesi şemaya (9 migration) gerçek veri yazıldı (client + anamnez + not + belge + storage nesnesi), ardından bekleyen migration uygulandı. Önce/sonra sayaçlar **birebir aynı**; `first_name`, `status='followup'`, `profile_extra.diagnoses`, not içeriği ve `is_pinned` korundu | **PASS** |
| (aynı test) yeni tablolar RLS ile doğuyor | `pg_class.relrowsecurity = true` hem `formulations` hem `safety_plans` için | **PASS** |
| (aynı test) mevcut RLS zayıflamadı | `clients_select` politikası hâlâ tek ve `created_by = auth.uid()` içeriyor | **PASS** |
| `re-running the migrations does not fail or duplicate anything` | Son 3 migration tekrar uygulandı (push retry simülasyonu): hata yok, satır sayıları aynı, **duplicate politika yok** (`group by policyname having count(*) > 1` → boş) | **PASS** |

**Ölçüm sırasında bulunan test hatası:** DDL `authenticated` rolüyle çalışmıyor —
`permission denied for schema public` / `must be owner of table clients`. Bu doğru davranış;
`db push` migration sahibi rolüyle bağlanır. Test `asSuperuser()` ile düzeltildi.

---

## 5. Auth verification

# `BLOCKED` — canlı Supabase Auth'a ulaşılamadı

| Kontrol | Durum |
|---|---|
| Psikolog A login / session / refresh / logout | **BLOCKED** |
| Psikolog B login / session / refresh / logout | **BLOCKED** |
| Admin login / session / refresh / logout | **BLOCKED** |
| Yanlış parola reddi | **BLOCKED** |
| Logout sonrası protected data erişimi | **BLOCKED** |

Test kullanıcıları **oluşturulmadı** — oluşturulacak bir Auth servisi yok. Gerçek kişilerin hesabı
kullanılmadı. Production'a test hesabı bırakılmadı.

Admin rolü mevcut sistemde SQL ile atanıyor (`20260924000000_initial_schema.sql:70` —
"First Admin bootstrapped once via SQL: `update profiles set role='ADMIN'`"). Bu yöntem canlı
ortamda denenemedi.

---

## 6. Client verification — `BLOCKED`
## 7. Anamnesis verification — `BLOCKED`
## 8. Appointment verification — `BLOCKED`
## 9. Appointment → Session — `BLOCKED`
## 10. Test results — `BLOCKED`
## 11. Reports — `BLOCKED`

Bu bölümlerin tamamı canlı Supabase gerektiriyor ve **hiçbiri çalıştırılmadı**.
Hiçbiri için PASS yazılmadı, tahmin edilmedi.

Karşılıklarında PGlite kanıtı §14 ve §15'te ayrı sütun olarak verildi — **canlı kanıt yerine geçmez.**

---

## 12. Formulation — `BLOCKED` (canlı)

| Gereken test | Canlı Supabase |
|---|---|
| 1 · refresh → veri gelmeli | **BLOCKED** |
| 2 · logout/login → veri gelmeli | **BLOCKED** |
| 3 · localStorage temizle → cloud pull | **BLOCKED** |
| 4 · B aynı formulation ID'yi açamaz | **BLOCKED** |
| 5 · B UPDATE reddedilmeli | **BLOCKED** |
| 6 · B DELETE reddedilmeli | **BLOCKED** |
| 7 · B, A'nın danışanına INSERT yazamaz | **BLOCKED** |

PGlite karşılığı `tests/cloudFormulationSafety.test.ts`'te 13/13 PASS (§15) — bu **canlı PASS değildir.**

---

## 13. Safety Plan — `BLOCKED` (canlı)

Aynı 7 test `safety_plans` için de **BLOCKED**. PGlite karşılığı 13/13 PASS (§15).

---

## 14. RLS matrix

### Canlı Supabase (§19'un istediği tablo)

| Veri | A SELECT | A UPDATE | B SELECT | B UPDATE | B DELETE | B INSERT |
|---|---|---|---|---|---|---|
| clients | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| anamneses | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| appointments | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| sessions | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| test_results | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| reports | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| documents | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| formulations | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| safety_plans | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| storage.objects | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |

Hiçbir hücre tahminle doldurulmadı.

### Local / PGlite (ayrı kanıt — canlı yerine geçmez)

| Veri | A SELECT | A UPDATE | B SELECT | B UPDATE | B DELETE | B INSERT | Kanıt |
|---|---|---|---|---|---|---|---|
| clients | PASS | PASS | PASS (0 satır) | PASS (0 satır) | PASS (0 satır) | PASS (reddedildi) | cloudWorkflow, cloudIsolation |
| anamneses | PASS | PASS | PASS | PASS | PASS | PASS | cloudWorkflow |
| appointments | PASS | PASS | PASS | PASS | PASS | PASS | cloudWorkflow |
| sessions | PASS | PASS | PASS | PASS | PASS | PASS (+ FK/trigger) | cloudWorkflow, cloudIsolation |
| test_results | PASS | PASS | PASS | PASS | PASS | PASS | cloudIsolation |
| reports | PASS | PASS | PASS | PASS | PASS | PASS | cloudWorkflow |
| documents | PASS | PASS | PASS | PASS | PASS | PASS | cloudWorkflow, migrationSafety |
| formulations | PASS | PASS | PASS | PASS | PASS | PASS | cloudFormulationSafety |
| safety_plans | PASS | PASS | PASS | PASS | PASS | PASS | cloudFormulationSafety |
| storage.objects | PASS | PASS | PASS | PASS | PASS | PASS | cloudIsolation (P0.5 düzeltmesi sonrası) |

---

## 15. IDOR

**Canlı Supabase: `BLOCKED`** — hiçbir IDOR denemesi canlı ortamda yapılmadı.

**Local / PGlite: PASS** (130 → 136 testin içinde):

| Hedef | SELECT | UPDATE | DELETE | INSERT |
|---|---|---|---|---|
| `client_id` | 0 satır | 0 satır | 0 satır | reddedildi |
| `anamnesis_id` | 0 satır | — | — | — |
| `appointment_id` | 0 satır | 0 satır | 0 satır | — |
| `session_id` | 0 satır | 0 satır | 0 satır | FK/trigger reddi |
| `test_administration_id` / `test_result_id` | 0 satır | 0 satır | 0 satır | — |
| `report_id` | 0 satır | 0 satır | 0 satır | — |
| `document_id` | 0 satır | 0 satır | 0 satır | — |
| `formulations.id` | 0 satır | 0 satır | 0 satır | reddedildi |
| `safety_plans.id` | 0 satır | 0 satır | 0 satır | reddedildi |
| `storage.objects` (path biliniyorken) | 0 satır | 0 satır | 0 satır | reddedildi |

UPDATE/DELETE iddialarının tamamı `returning` ile ölçüldü (RETURNING olmadan iddia boş olur).

---

## 16. Storage

**Canlı Supabase: `BLOCKED`** — gerçek private bucket'a dosya yüklenmedi, `storage.objects` RLS'i canlı
ortamda test edilmedi. P0.5'te bulunan açığın **production'da kapalı olduğu kanıtlanmadı.**

**Local / PGlite: PASS** — `tests/cloudIsolation.test.ts` §4/§5, `tests/migrationSafety.test.ts` §18.
P0.5 düzeltmesi (`20260925000001`) bu fazda **değiştirilmedi**; politikalar
`pg_policies` üzerinden doğrulandı.

---

## 17. Signed URL

# `BLOCKED`

P0.5 ve P0.6'da BLOCKED'tı, **bu fazda da BLOCKED.** Hiçbir gerçek signed URL üretilmedi.

| Kontrol | Durum |
|---|---|
| 1 · A yetkili dosya için signed URL üretebiliyor mu | **BLOCKED** |
| 2 · URL dosyayı gerçekten indirebiliyor mu | **BLOCKED** |
| 3 · B yetkisiz dosya için signed URL alabiliyor mu | **BLOCKED** |
| 4 · B'nin doğrudan URL erişimi engelleniyor mu | **BLOCKED** |
| 5 · logout sonrası URL davranışı | **BLOCKED** |
| 6 · expiry gerçekten çalışıyor mu | **BLOCKED** |

Sebep: canlı Storage servisi yok; PGlite'ta nesne deposu yok. Test helper'ındaki `signedUrl`
**stub**'dır ve öyle kaldı (`https://signed.invalid/…`) — gerçekmiş gibi raporlanmadı.
Stub veya mock ile PASS üretilmedi.

---

## 18. LocalStorage clear / recovery

**Gerçek browser + gerçek Supabase: `BLOCKED`.**

**Local / PGlite: PASS** — `tests/cloudStoreSync.test.ts` (9 varlık) ve
`tests/cloudFormulationSafety.test.ts` (formulation + safety plan). Her ikisinde de
`storageMap.clear()` sonrası yerel okuyucuların **0 kayıt** döndürdüğü doğrulanıyor, ardından pull
ile tüm alanlar geri geliyor. Yani localStorage'ın kaynak veri olmadığı **entegrasyon seviyesinde**
kanıtlı; **browser + canlı Supabase seviyesinde kanıtlı değil.**

---

## 19. Production Auth Gate

Bu fazda **statik analizden gerçek çalıştırmaya yükseltildi.**

### Yeni: gönderilen artifact'in DOM'da çalıştırılması

`tests/helpers/runProductionBundle.mjs` — `dist/assets/` içindeki **gerçek dosyalar** jsdom window'unun
VM context'inde `vm.SourceTextModule` olarak evaluate ediliyor; React gerçekten mount oluyor.

Sonuç (`.env` olmadan build edilmiş bundle):

```
VAR   Çalışma alanı hazır değil          →  notReady   : true
yok   Bugünün tahtası (dashboard)        →  dashboard  : false
yok   Danışan Dosyaları (clients)        →  clients    : false
yok   Vaka formülasyonu                  →  formulation: false
yok   Güvenlik planı                     →  safetyPlan : false
yok   Raporlar / Randevular              →  reports/appointments: false
yok   Hoş geldiniz (login)               →  login      : false
yok   Kayıt Ol                           →  publicSignup: false
fetch çağrıları: yalnızca kendi 2 chunk'ı (modulepreload) — Supabase'e 0 istek
console hataları: 0
```

**Test boş değil — mutation ile doğrulandı.** Bundle'daki kapı geçici olarak
`function g0(s){return "local-dev"}` hâline getirildiğinde harness şunu üretti:

```
notReady: False      dashboard: True
```

Yani klinik dashboard auth'suz açıldı ve test FAIL olurdu. Ardından `npm run build` ile geri alındı
(kapı yine `function g0(s){return s.configured?"cloud":"not-ready"}`).

### Statik kanıt (mevcut, korundu)

`tests/build.test.ts` → `production bundle cannot bypass authentication`:
kapının `isDev` parametresi yok, `isDevRuntime` 0 adet, `import.meta.env` 0 adet, `.DEV` 0 adet,
`service_role` 0 adet.

### Development davranışı

`tests/devLocalAccess.test.ts` — dev-mod Vite sunucusunda `isDevRuntime === true`,
`configured === false` → `local-dev`, çalışma alanı açılıyor. **Değiştirilmedi.**

---

## 20. Browser E2E

# `BLOCKED — browser environment unavailable`

`e2e/critical.spec.ts` **çalıştırılmadı.** P0.5'ten bu yana durum değişmedi, bu fazda yeniden ölçüldü:

```
/tmp/chromium mevcut (P0.5'te @sparticuz/chromium@153.0.0 ile kuruldu, çalıştırıldı)
ldd /tmp/chromium | grep "not found":
  libnspr4.so      => not found
  libnss3.so       => not found
  libnssutil3.so   => not found
deb.debian.org     => HTTP 000   (apt-get update: Connection failed)
cdn.playwright.dev => HTTP 000
```

§22 kuralı gereği **rastgele binary / doğrulanmamış `.so` indirilmedi.** `codeload.github.com`
erişilebilirdi; oradan NSS kütüphanesi çekmek tedarik zinciri riski oluşturacağı için yapılmadı.

Not: jsdom bir **browser değildir** — §19'daki çalıştırma gerçek layout/paint/router olmadan, yalnızca
DOM + JS seviyesinde yapılır. E2E yerine geçmez ve öyle raporlanmadı.

---

## 21. Typecheck

```
npm run typecheck → tsc --noEmit → temiz
```
**PASS** (başında ve sonunda çalıştırıldı).

---

## 22. Full test suite

```
npm test → # tests 136  # pass 136  # fail 0
```

**Test sayısı 130 → 136 (+6).** Nedeni:

| Dosya | Eklenen |
|---|---|
| `tests/migrationSafety.test.ts` (yeni) | +5 (1 parent + 4 subtest) |
| `tests/build.test.ts` (genişletildi) | +1 (production bundle'ı DOM'da çalıştıran test) |

Hiçbir test silinmedi, hiçbir assertion zayıflatılmadı. Mevcut testlerdeki tek değişiklik
`tests/helpers/pglitePort.ts`'e **ek** seçenekler (`upToMigration`, `applyRemainingMigrations`,
`reapplyMigration`) — varsayılan davranış aynı, mevcut 20 cloud testi değişmeden PASS.

---

## 23. Build

```
npm run build → ✓ built in 2.86s
dist/index.html                     1.47 kB │ gzip:   0.72 kB
dist/assets/index-DhRHW3cU.css    143.93 kB │ gzip:  26.05 kB
dist/assets/vendor-DPIr46qh.js     11.88 kB │ gzip:   4.20 kB
dist/assets/supabase-B5gRX2w9.js  223.38 kB │ gzip:  58.43 kB
dist/assets/index-BS_3veIg.js     487.13 kB │ gzip: 136.86 kB
```
**PASS.** Bundle P0.6 ile aynı boyutta (src/ değişmedi).

Deploy: bu fazda yeni deployment mimarisi kurulmadı. Gerçek production URL üzerinde browser testi
yapılamadı → **tahmin edilmedi.** Build'in deploy edilmeye hazır olduğu yalnızca build çıktısıyla doğrulandı.

---

## 24. Changed files

**Yeni — 3:**
```
tests/migrationSafety.test.ts              (+218)  db push güvenliği + §18 file_path/delete
tests/helpers/runProductionBundle.mjs      (+109)  gerçek bundle'ı DOM'da çalıştıran harness
docs/P0.7-REPORT.md                        (bu rapor)
```

**Değiştirilen — 4:**
```
tests/build.test.ts        (+52)   production bundle'ı DOM'da çalıştıran test
tests/helpers/pglitePort.ts (+59/-9) createTestDatabase({upToMigration}) + applyRemainingMigrations + reapplyMigration
package.json               (+1)    devDependencies: jsdom ^30.1.1
package-lock.json          (+542)  jsdom ağacı
```

Toplam: 6 dosya, +972 / −9.

**`src/` altında HİÇBİR dosya değişmedi.** Migration'lara dokunulmadı. UI değişmedi.
P0.6 kodu geri alınmadı.

**Bağımlılık:** `dependencies` **değişmedi** — hâlâ tam olarak 3 paket
(`@supabase/supabase-js`, `react`, `react-dom`). `jsdom` yalnızca `devDependencies`'e eklendi
(§19'daki doğrulamayı kalıcı ve tekrarlanabilir yapmak için; kullanıcı onayı alındı).

---

## 25. Remaining blockers

### Bu fazı engelleyenler (ortam)
1. **Supabase erişimi** — `api.supabase.com` / `*.supabase.co` HTTP 000. Credentials ve project ref yok.
   Çözüm: ağ erişimi olan bir ortam + `SUPABASE_ACCESS_TOKEN` + doğrulanmış `project_id`.
2. **Browser** — `libnspr4.so`, `libnss3.so`, `libnssutil3.so` eksik; Debian mirror kapalı.
3. **Signed URL** — canlı Storage yok.

### Canlı ortamda koşulması gerekenler (erişim açılınca)
4. `supabase db push` (10 migration) + §4'teki yapıların remote'ta varlığının doğrulanması.
5. §5–§13: Auth, client, anamnez, appointment→session, test result, report, formulation, safety plan.
6. §14 RLS matrisinin Live sütunu + §15 IDOR.
7. §15–§17: Storage upload, `storage.objects` IDOR, signed URL (6 kontrol).
8. §18: gerçek browser'da localStorage temizleme + recovery.
9. §20: `e2e/critical.spec.ts` gerçek browser'da.

### Ürün tarafı (bu fazın kapsamı dışı, P0.5/P0.6'dan devralındı)
10. `psychologist_settings` yazma politikası kurum geneline açık.
11. `admin_list_profiles()` ORG_ADMIN'e tüm kurumları döndürüyor.
12. Not/seans/formülasyon kilitleme yok — son yazan kazanır.
13. Danışansız test sonucu/rapor senkronize edilemiyor (`client_id NOT NULL`).
14. Klinik veriler cihazda şifresiz (`tcNumber` dâhil; buluta gitmiyor ama localStorage'da).
15. CSP sapması: `public/_headers` kullanılıyor, `src/lib/securityHeaders.ts` import edilmiyor.

---

## 26. Final decision

# C — `P0.7 LIVE VALIDATION BLOCKED`

Gerekçe:
- Canlı Supabase'e **tek bir istek atılamadı** (`api.supabase.com` ve `*.supabase.co` HTTP 000).
- Supabase CLI kuruldu ve çalıştırıldı; **login mümkün değil** (non-TTY, token yok, API erişilemez).
- **Project ref doğrulanamadı** → §4 kuralı gereği hiçbir projeye bağlanılmadı, `db push` yapılmadı.
- Gerçek browser **çalıştırılamadı** (3 sistem kütüphanesi eksik, mirror'lar kapalı).
- Signed URL **üretilmedi**.

A ve B seçilmedi çünkü ikisi de canlı Supabase PASS gerektiriyor ve o kanıt yok.
**Tahminle A veya B seçilmedi.**

Bu fazın ürettiği gerçek kazanımlar (canlı kanıt değildir):
- `db push` güvenliği: dolu veritabanına uygulama + retry idempotency — **5/5 PASS**
- Production bundle'ın DOM'da çalıştırılması: auth'suz hiçbir klinik ekran açılmıyor — **PASS**, mutation ile doğrulandı
- typecheck PASS · **136/136** PASS · build PASS

---

## 27. Kanıt ayrımı (§29)

```
PGlite PASS                    ≠  Live Supabase PASS
  136/136 PASS, gerçek             0 istek — BLOCKED
  migration'lar, mock yok

Unit/Integration PASS          ≠  Browser E2E PASS
  136/136 PASS                   e2e/critical.spec.ts 0 test çalıştırdı — BLOCKED

RLS migration test PASS        ≠  Production RLS PASS
  politikalar pg_policies'te       canlı projede policy sorgulanmadı — BLOCKED
  doğrulandı, IDOR 0 satır

Storage policy PASS            ≠  Signed URL PASS
  storage.objects RLS PASS         signedUrl stub, hiç üretilmedi — BLOCKED
```

Ek ayrım: **DOM'da bundle çalıştırma ≠ Browser E2E.** jsdom layout, paint, gerçek router geçmişi ve
gerçek ağ katmanı sağlamaz; §19'daki sonuç yalnızca "gönderilen JS artifact'i bu DOM'da bu ekranı
render etti" anlamına gelir.
